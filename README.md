
  # High Fidelity Prototype

  This is a code bundle for High Fidelity Prototype. The original project is available at https://www.figma.com/design/H6S8ViECd3Bzc14s5Bn4KQ/High-Fidelity-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  "# Pangea" 
